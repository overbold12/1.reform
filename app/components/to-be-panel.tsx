import { EmptyWorkspace } from "./empty-workspace";

export function ToBePanel() {
  return (
    <EmptyWorkspace
      label="TO-BE 앱 화면"
      description="향후 모바일 앱 프로토타입이 배치될 기본 작업 영역입니다."
    />
  );
}
